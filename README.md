# octopus 智能体

`octopus` 多智能体协同完成任务

## 主功能

- 工作流程

```
任务进入 → 看板分区 → 检测无 Agent → 孵化子 Agent → 执行任务 → 完成任务 → 触发重平衡 → 检测无任务 → 释放子 Agent → 汇报状态
```

1. 弹性生命周期管理 (Auto-Scaling)
   
  * 零任务自动释放：看板区域任务完成后自动销毁子 Agent 实例
  * 按需自动孵化：新任务进入时动态启动新的子 Agent 实例
  * 主 Agent 常驻：作为系统核心守护进程持续监控调度

3. 看板驱动的任务流 (Kanban-Driven Workflow)
   
  * 区域隔离：各功能区域独立运行
  * 状态实时同步：任务状态变更实时发布到事件总线
  * 双向通信：主动汇报和被动查询机制

4. 自主协调与规划
   
  * 智能路由：主 Agent 自动拆分分发任务到对应看板区域
  * 上下文隔离：每个子 Agent 专注当前区域任务

5. 周期任务支持
   
  * 集成 cron 模块，支持看板上的周期性任务


## 待完善内容

1. 子 Agent 执行引擎 (pkg/kanban/agent_worker.go)
   
  * 每个子 Agent 拥有独立的执行协程，持续监听所属区域的待处理任务
  * 自动从看板区域拉取任务，更新任务状态
  * 支持配置每个区域的最大并发任务数
  * 响应 Context 取消信号，优雅退出
  
2. 动态模板加载器 (pkg/kanban/loader.go)
  
  * 从文件系统或嵌入式资源加载 agent.md 模板
  * 自动将区域名称、任务上下文、全局配置注入到 Prompt 模板
  * 对加载后的 Agent 配置进行内存缓存
  * 支持热重载功能

3. 事件驱动集成增强
  
  * 主动汇报：任务状态变更、Agent 生灭事件实时推送到所有连接的 Channel
  * 被动查询：监听来自 Channel 的指令，实时返回看板快照
  * 集成 cron，支持创建周期性任务，自动触发子 Agent 孵化
